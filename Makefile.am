SUBDIRS = chafa docs libnsgif tools

EXTRA_DIST = \
	README.md \
	autogen.sh \
	chafa.pc.in

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = chafa.pc

all-local:
	$(info )
	$(info ---)
	$(info --- Success! You can now run tools/chafa/chafa, or install everything)
	$(info --- using "make install" or "sudo make install".)
	$(info ---)
	$(info )
